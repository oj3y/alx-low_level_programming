#include <stdio.h>
/**
 * main -> assign random number into n as it is executed and print
 * the last digit of the number stored in n
 */
int main(void)
{
int n;

for (n = 0; n < 10; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
